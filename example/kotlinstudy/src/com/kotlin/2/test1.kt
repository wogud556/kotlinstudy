package com.kotlin.`2`

fun test1(){
    var collection = listOf(1 , 2, 3 ,4,5)
    collection.iterator()
    for(item in collection)
        print(item)

    val array = arrayOf(1,2,3,4,5)
    array.iterator()
}